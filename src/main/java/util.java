package src.main.java;

public class util {
    public static class statusCodes {
        public enum dbStatus {
            OK,
            FAILED,
            UNAUTHORISED,
            INVALID_ARGUMENTS
        }
    }
}